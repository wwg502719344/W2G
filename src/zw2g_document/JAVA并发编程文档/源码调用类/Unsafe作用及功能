unsafe描述
这个类用来执行低等级的，不安全操作的方法集合，虽然他的执行方法都是public，但是使用这个方法也是受限的，该类'只
能给被进行安全授权的类使用

CAS方法的实现是通过传递内存偏移量，新老值进行判定的，内存偏移量可以通过指定的方法获取

AtomicInteger是原子操作变量，内部实际就是通过使用unsafe达到原子操作的，一般可用作计算器的使用
优点是原子操作，缺点就是内部的实现调用了大量的unsafe类，通过循环的方式保证操作的正确行，如果
并发比较大的情况下，就会进行较多的重试，效率就会比较低