LinkedBlockingQueue
M:线程池fixThreadPool中采取的存储元素的队列,该队列内部采用的是单链表式存储元素，head出，tail进，内部使用了reentLock
进行数据并发安全性的保护，所以，该队列是并发安全的

CachedThreadPool
Q:该线程池使用的队列是SynchronousQueue队列，该队列的特性是什么
A:该队列没有任何内部容量，每一个插入操作必须等待对应线程的移除操作，仅在该移除的时候,传球手的性质，该队列中
创建的线程，在一分钟之内可以复用，该线程池核心线程为0，最大线程数为最大，为无界线程池
该线程池的意义就是，当任务产生的速度大于任务执行的速度的时候，可以有源源不断的线程去执行产生的任务，该线程
池适合的场景是任务量大但是耗时短的操作

