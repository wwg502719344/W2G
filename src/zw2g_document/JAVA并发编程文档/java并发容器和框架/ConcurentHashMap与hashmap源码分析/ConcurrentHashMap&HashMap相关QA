Q1:为什么说hashmap是线程不安全的，而ConcurrentHashMap是线程安全的

Q2:hashmap,hashtable,ConcurrentHashMap之间的关系和区别

Q3:根据hash值计算的位置是否是实际key节点的位置

Q4:为什么在ConcurrentHashmap中value和next是volatile的，而hashmap中不是

Q5:hashcode和hash之间的关系




A1:因为hashmap在多线程下可能会造成环链和数据丢失问题，Con将数据存在node中
，而node的key是不可变量，value和next是volatile类型，保证了数据在多线程
模式也也是线程安全的

